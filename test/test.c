/**
 * case 'i':
 * case 'd':
 *	val = va_arg(ap, int);
 *	if (val < 0)
 *	{
 *		val = -val;
 *		_putchar('-');
 *		sum++;
 *	}
 *	converted = convert(val, 10);
 *	_puts(converted);
 *	sum += xlen(converted);
 *	break;
*/